ul#categoryList
  - @categories.each do |category|
    li = category.name

ul#productList
  - @products.each do |product|
    li = product.name
    ul
      li = product.price
      li = product.description
